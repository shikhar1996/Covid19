package logger

/*
import (
	"github.com/uber-go/zap"
)

var Logger *zap.Logger

func Init() {
	Logger, _ := zap.NewDevelopment()
	// Logger, _ := zap.S()
	defer Logger.Sync()
	zap.ReplaceGlobals(Logger)
}
*/
